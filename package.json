{
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "oxfmt packages/ fixtures/",
    "fmt:check": "oxfmt --check packages/ fixtures/",
    "lint": "oxlint --deny-warnings packages/ fixtures/",
    "build": "pnpm -C packages/graphgarden-web run build",
    "test:e2e": "cd fixtures && pnpm test"
  },
  "devDependencies": {
    "oxfmt": "^0.33",
    "oxlint": "^1"
  },
  "packageManager": "pnpm@10.29.1"
}
